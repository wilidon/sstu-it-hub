name: SSH Deployment

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install SSH client
      run: sudo apt-get install -y openssh-client

    - name: SSH Command
      run: |
        sshpass -p ${{ secrets.SSH_PASSWORD }} ssh root@147.45.138.159 'cd /root/it-hub/sstu-it-hub && \
                                  git pull && \
                                  docker-compose down && \
                                  docker-compose up -d --build'
